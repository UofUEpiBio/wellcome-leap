# Generated model artifacts

R `torch` weights, preprocessing metadata, and evaluation outputs are written here during training. Generated artifacts are ignored by Git and must not be committed.

This includes multiscale-emulator weights, preprocessing metadata, and
leave-site-out evaluation objects. The published `app/model.json` remains a
separate, explicitly approved export.
